# includes/setup/configs/mail/rspamd/modules.conf.template

# This is a rendered file for domain {{IWB_DOMAIN}}
# Please do not edit here, this template is built into the docker container

# Keep the packaged module-loading chain intact so modules.d defaults and
# local.d/override.d customizations merge as Rspamd expects.
.include(glob=true) "${CONFDIR}/modules.d/*.conf"
